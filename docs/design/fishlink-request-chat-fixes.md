# 【実装依頼】配送管理チャット 実装修正（農家側・2026-06-17）

下道さん

配送管理チャットを実装で確認しました。**chat-specどおり忠実に反映OK**でした、ありがとうございます🙏（システム行・両側の出し分け・買い手側＝入力のみ・距離のみ地図リンクなし 等）。**買い手側（レストラン）は修正なし**。修正は**全部 農家側**で5点です。

仕様の正本：
- 仕様書：**`fishlink-chat-spec.md`**（§4 音声・§5 ステータス送信・§10 配送完了・§11 差分の「2026-06-17」表）
- モック：**`fishlink-chat-mockup.html`**（音声＝▶＋簡易波形＋秒数 のカスタム表示が正・配送完了の用語も正）

> 音声と配送完了の用語は**モックが元から正しく、実装だけがズレ**ています（native音声プレーヤー／「配達完了」）。モックに合わせてください。

---

## 修正点（農家側・5つ）

| # | 箇所 | 現状 | 修正後 |
|---|------|------|--------|
| ① | ステータス送信（準備開始/配送開始/到着/少し遅れそう） | 押すと**即時にチャット投稿＋プッシュ** | **送信前に確認ボトムシート**「📦 ◯◯を送信しますか？ 送信／やめる」を出す（押した瞬間には飛ばない）。＝間違いの不安を消す・配送完了と同じ確認パターンで統一。確認系は全部ボトムシート（承認/辞退/キャンセル/支払い完了/配送完了/ステータス送信） |
| ② | 「配送完了」確認モーダル | 中央モーダル | **ボトムシートに**（内容「配達完了にしますか？レストランにお支払い…」はOK） |
| ③ | 用語 | ボタン「配送完了」／モーダル「**配達**完了にしますか？/配達完了にする」で混在 | **「配送完了」に統一**（モーダルも「配送完了に…」に） |
| ④ | ボイスメッセージUI | **ブラウザ標準の音声プレーヤー（native `<audio>`）**＋緑ボタン＋中央プレビューモーダル。録音→**停止→プレビュー→送信** | **(a) 録音→送信に簡略化**：録音バーを「破棄／送信」に・**送信＝停止＋送信を1発**・**プレビューモーダルを廃止**（聞き直しは送信後の自分のバブル再生でOK）／**(b) 送信済みは「▶＋簡易波形＋秒数」のカスタム表示**（native `<audio>`をやめる・モックの`.voice`どおり）／**(c) 停止・送信ボタン 緑→青・破棄＝赤 or 中立グレー** |
| ⑤ | 写真の複数添付プレビュー | **✕が1つ＝全部まとめて消える** | **1枚ごとに消せる**（各サムネの右上に小さい✕）。＝養殖環境/お店の様子の複数写真管理と同じ。**送信は縦並びのまま維持**（1枚ずつ大きく＝魚の鮮度/量が見やすい・グリッドは小さくなるので不採用） |

---

## 範囲外・補足

- **買い手側（レストラン）チャット＝修正なし**（フッター入力のみ・ヘッダー「配達中」・距離のみ・📞通話 ＝すべて正しい）。
- **📞通話**＝実番号・進行中のみのMVP割り切り（chat-spec §9）でそのまま。将来マスク通話に昇格。
- システム行（配送開始/少し遅れそうですamber/到着しました。お受け取りください）＝実装OK・修正不要。
- テストデータ（test_restaurant5 等）は本番差し替え前提。
