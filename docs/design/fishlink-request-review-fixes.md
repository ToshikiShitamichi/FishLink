# 【実装依頼・修正】レビュー（実装レビューの差分）（2026-06-27）

下道さん

レビュー画面（買い手→農家／農家→レストランの両側）を実機で確認しました。**実装はかなり忠実で、修正は少しだけ**です。

正本：仕様書 `fishlink-review-spec.md` ／ モック `fishlink-review-mockup.html`（⑤送信完了を追加）

## ✅ 実装OK（修正不要・確認できた点）
- 3軸の役割別（買い手→農家＝取引の満足度/魚の品質/配送時間・農家→レストラン＝総合評価/やり取り/受取対応）。
- **バリデーション＝インライン＋下部総括の両方とも実装済み**（未選択軸を赤枠＋「選択してください」＋「未選択の項目があります」）。
- 線画スマイリー／未選択グレー・良かった緑・残念だった赤／上部ガイド／3軸1カード区切り線／残念だった→コメント促し（amber）。
- コメントの**連絡先マスク**＝**部分マスクで動作確認済み**（例「よかったです。電話番号は［連絡先は非表示］です。」＝**本文は残り連絡先だけ伏字**）。電話番号のみのコメントは全体が「［連絡先は非表示］」＝正常。送信はブロックしない（表示でマスク）＝仕様どおり。
- ※評価対象の「farmer5／test_restaurant5」はテストデータ（本番は実名）＝修正不要。

## 🔧 修正
| # | 画面 | 現状 | 修正後 |
|---|------|------|--------|
| 1 | 送信完了 | 〔戻る〕ボタンが**ダークネイビー** | **青 `#0B5FB0`**（CTAは青の規則・この画面だけ色が外れている） |
| 2 | 送信完了 | ボタン文言「**注文一覧へ戻る**」だが実際の遷移先と名前が不一致（**農家＝注文確認／買い手＝注文状況**に着地） | **ラベルを実際の遷移先に合わせる**（農家＝「注文確認へ戻る」／買い手＝「注文状況へ戻る」、or 単に「戻る」）。来た画面に戻すのが自然 |
| 3 | レビュー入力（買い手→農家） | コメントの placeholder が「良かった点などを書いてください」固定 → 残念だった選択時に文脈とズレ | **中立に統一「良かった点・気になった点など」**（モック反映済み）。**文脈での出し分けは不要**（amber促しが補う） |

## ✅ モデレーションも確認済み
- マスク発動コメントが**管理画面「トラブル報告」に「レビュー（要確認）」として自動で上がる**＋〔投稿を非表示にする〕で🗑非表示できる＝**spec §9 のモデレーションどおり動作**。修正不要。
- （部分マスクも上の✅で動作確認済み＝マスク／モデレーションとも実装OK。）

## 別途（運営管理画面レビューで）
- トラブル報告の「関連注文」が**内部のランダムID**（例 GN10CpnET3cG4UmqRb7Q）表示 → **FL番号**（FL-…）に（運営の照合用）。

## 別途（ヘッダー整理でまとめて）
- レビュー後の遷移先（注文確認等）のヘッダーに **✓✓（やることリスト）** が出ているが、**やることリストはMVPで作らない＝ヘッダーから外す**（他のヘッダー整理とまとめて）。
